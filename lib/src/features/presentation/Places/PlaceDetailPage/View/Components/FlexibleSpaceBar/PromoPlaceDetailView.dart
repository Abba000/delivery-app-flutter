// ignore_for_file: file_names

import 'package:flutter/material.dart';

class PromoPlaceDetailView extends StatelessWidget {
  const PromoPlaceDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 50,
      color: Colors.transparent,
    );
  }
}
